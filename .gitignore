/bin/
/dist/
# `go build ./demo/...` writes the recorder here.
/record
# `go build ./hack/<tool>` writes the tool here. Two were committed that way;
# hack/tracked_binaries_test.go refuses any compiled executable in the tree.
/crdreference
/unknownlayerfixture
/cover.out
/coverage/
/.idea/
/.vscode/
.DS_Store
*.test
*.prof
*.log

# The demonstration lab writes its kubeconfig, environment and logs here.
/demo/.lab/

# The link checker writes its self-test fixture here.
/docs/site/.selftest-links/
